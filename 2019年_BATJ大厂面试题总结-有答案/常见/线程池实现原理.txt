线程池实现原理
	首先写一个实现Runable的类

	然后在创建有个类  给当前类添加上线程类的泛型
	添加有个最大连接上限的常量
	在添加一个线程任务列表  这里我用的是LinkedList
	在创建一个线程列表  用的ArrayList
	在添加一个执行线程数量的变量 默认不赋值
	
	先写一个内部类并实现Runable
	定义一个当前线程状态
	重写run方法
	然后while循环状态
	线程列表加锁
	然后   任务列表.wait()  等待唤醒线程
	然后 任务列表.removeFirst 然后用对象接收  然后.run 启动
	
	写构造函数   他有一个参数 就是线程池默认创建多少线程  然后根据执行线程列表进行初始化并放到线程列表中
	
	
	现在添加执行方法（有个入参  是线程类）
	任务列表.addLast(线程类添加进去);
	任务列表.notify();